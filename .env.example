# Anthropic
ANTHROPIC_API_KEY=sk-ant-your-key-here

# JWT
JWT_SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
MOCK_API_BASE_URL=http://localhost:8000

# ChromaDB (swap → OpenSearch Serverless in prod)
CHROMA_PERSIST_DIR=./chroma_db

# SQLite (swap → DynamoDB in prod)
SQLITE_DB_PATH=./recruiting.db

# GitHub MCP (stub — set real values for next phase)
GITHUB_TOKEN=ghp_your_token_here
GITHUB_REPO=your-username/conversational-analytics-ai

# Frontend
VITE_API_BASE_URL=http://localhost:8000
